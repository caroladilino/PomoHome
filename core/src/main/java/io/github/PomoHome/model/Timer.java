package io.github.PomoHome.model;

/**
 * Pomodoro timer.
 *
 * Empty stub on purpose — the team already has timer logic that will be
 * pasted in here. Leave the public surface (constructor + a few obvious
 * methods like start(), pause(), reset(), getSecondsLeft()) to be filled
 * in by whoever owns the timer code.
 *
 * Once it's filled in, hook it up like this in TelaPomodoro:
 *   - on tick: redraw the digits
 *   - on complete: call apiClient.registrarSessao(jogadorId, minutos, ...)
 */
public class Timer {
    // TODO: TEAM — paste existing timer logic here.
}
